package view;

import model.components.Grid;

/**
 * The essential view methods.
 */
public interface View {

	/**
	 * It shows the grid.
	 * 
	 * @param grid the grid
	 */
	void showGrid(Grid grid);

}